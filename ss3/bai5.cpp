#include<stdio.h>
#include <string.h>
int isPalindrome(char str[], int start, int end) {
//	dieu kien dung
	if (start >= end) {
        return 1; 
    }
}
int main(){
	char str[100];

//  nhap chuoi
    printf("Nhap chuoi: ");
    scanf("%99[^\n]", str); 

    int length = strlen(str); // tinh do dai
}
